# Riot Games API key (Development keys expire after 24 h, Personal/Production keys do not).
# Only needed from phase 4 on (Match-V5 timelines, live game search). Never commit the real key.
RIOT_API_KEY=
